import * as React from 'react';
import {RouteComponentProps} from 'react-router';

export default function NotFunction(_: RouteComponentProps<any>) {
    return (
        <div>
            Not Found
        </div>
    )
}